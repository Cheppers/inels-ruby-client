{
    "device info": {
        "product type": "HeatCoolArea",
        "type": "temperature regulation area"
    },
    "schedule": "<%= @schedule %>",
    "temperature sensor": {
        "<%= @temperature_sensor %>": "temperature"
    },
    "heating devices": [
        <% @heating_devices.each do |heating_device| %>
            {
                "id": "<%= heating_device %>",
                "central source": true
            },
        <% end %>
    ]
}